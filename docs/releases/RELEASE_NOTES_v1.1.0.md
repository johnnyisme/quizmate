# QuizMate v1.1.0 Release Notes

**發布日期**: 2026 年 1 月 3 日

---

## 🎉 重點新功能

### 多圖片上傳支援
現在可以在同一個對話中上傳多張不同的圖片，不需要重新開始對話！

**使用場景**：
- 第一張圖片問數學題 → 得到解答
- 第二張圖片問英文題 → 繼續對話
- 完整對話歷史保留，方便回顧學習過程

**技術亮點**：
- ✅ Image Reference Pattern：防止圖片上傳時的 race condition
- ✅ 智慧預覽顯示：
  - 空對話：中央大預覽區
  - 有對話：輸入框上方顯示 80px 縮圖預覽
- ✅ File input 清空機制：支援重新選擇同一檔案
- ✅ Settings modal 友善設計：開關設定不會清除圖片預覽

---

## 🧪 測試品質提升

### 新增測試覆蓋
- **新增測試檔案**: `multiImageUpload.test.tsx` (11 tests)
- **測試類別**:
  - 同 Session 上傳 (2 tests)
  - File Input 清除機制 (1 test)
  - 圖片預覽顯示邏輯 (2 tests)
  - 圖片狀態管理 (4 tests)
    - Settings modal 保留預覽
    - Image Reference Pattern
    - 頁面 reload 清空
    - Session 切換清空
  - 錯誤恢復 (1 test)
  - 初始載入標記 (2 tests)

### 測試統計
- **總測試數**: 1,085 tests（42 個測試檔案）
- **測試狀態**: ✅ 全部通過 (1085/1085)
- **測試分類**:
  - Unit tests: 984
  - Integration tests: 95
  - Regression tests: 2
  - E2E tests: 4
- **整合測試覆蓋率**: 8.8% (95/1085)
- **整體測試覆蓋率**: ~92%

---

## 📝 文件更新

### copilot-instructions.md
- 新增「Multi-Image Upload in Same Session」工作流程說明
- 更新 Image Reference Pattern 實作細節
- 說明條件式預覽顯示邏輯

### README.md
- 新增多圖片上傳功能介紹
- 更新使用說明和操作流程
- 補充功能特色說明

### TESTS.md
- 完整記錄測試覆蓋詳情
- 新增多圖片上傳測試章節
- 更新精確測試數量（1,085 tests）

---

## 🔧 技術改進

### Image Reference Pattern
防止圖片上傳時的 race condition：
```typescript
// 保存引用
const currentImage = image;
const currentImageUrl = imageUrl;

// 立即清空狀態（允許下次上傳）
setImage(null);
setImageUrl("");

// 使用保存的引用進行 API 呼叫
await fileToBase64(currentImage);
```

### 條件式預覽渲染
```typescript
{imageUrl && displayConversation.length > 0 && (
  // 只在有對話記錄時顯示輸入框上方的縮圖預覽
  <img src={imageUrl} className="h-20" />
)}
```

### Session 切換邏輯
```typescript
const handleSwitchSession = useCallback((sessionId: string) => {
  setCurrentSessionId(sessionId);
  // 清空圖片預覽（切換 session 時不保留）
  setImage(null);
  setImageUrl("");
  localStorage.setItem('current-session-id', sessionId);
}, []);
```

---

## 🐛 Bug 修復

- ✅ 修正：Settings modal 開關時錯誤清空圖片預覽
- ✅ 修正：檔案 input 無法重新選擇同一檔案
- ✅ 修正：頁面重載後錯誤恢復舊圖片預覽

---

## 📊 專案狀態

- **測試覆蓋率**: ~92%
- **程式碼品質**: All tests passing ✅
- **文件完整性**: 100%
- **TypeScript**: Strict mode enabled
- **框架版本**:
  - Next.js 16.1.1
  - React 19
  - Vitest 1.6.1
  - Tailwind CSS v4

---

## 🚀 部署建議

### 本地測試
```bash
npm install
npm test              # 執行所有測試
npm run build         # 建置專案
npm run start         # 啟動 production server
```

### Vercel 部署
1. 連接 GitHub repository
2. 無需設定環境變數（純前端專案）
3. 自動部署，使用者在瀏覽器中設定 API Keys

---

## 📖 升級指南

### 從 v1.0.0 升級
無需特殊操作，直接更新即可：
```bash
git pull
npm install
npm test
```

### 注意事項
- 圖片預覽為暫時性狀態，不會持久化
- Settings modal 開關不影響當前上傳的圖片
- Session 切換會清空未發送的圖片預覽

---

## 🙏 致謝

感謝所有貢獻者對 QuizMate 專案的支持！

---

**完整變更日誌**: [v0.1.0...v1.1.0](https://github.com/your-repo/quizmate/compare/v0.1.0...v1.1.0)
